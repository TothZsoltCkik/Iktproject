CREATE DATABASE menhely;
USE menhely;

CREATE TABLE menhely (
    id INT PRIMARY KEY AUTO_INCREMENT,
    nev VARCHAR(255),
    kor INT,
    szobatisztaE VARCHAR(255),
    tulajdonsagok VARCHAR(255)
);

